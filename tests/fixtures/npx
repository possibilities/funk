#!/bin/bash

set -u

if [ -n "${FUNK_TEST_NPX_LOG:-}" ]; then
    {
        printf 'npx-stub'
        printf ' <%s>' "$@"
        printf '\n'
    } >>"$FUNK_TEST_NPX_LOG"
fi

# Callers that need to see a failing skills tool set this; everything else gets
# the successful path after recording the invocation.
exit "${FUNK_TEST_NPX_EXIT:-0}"
