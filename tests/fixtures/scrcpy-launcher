#!/bin/bash

set -euo pipefail

printf '%s\n' "$@" >"${FUNK_TEST_SCRCPY_LOG:?}"
printf 'ADB_SERVER_SOCKET=%s\nADB_USB=%s\nADB_MDNS_AUTO_CONNECT=%s\n' \
    "${ADB_SERVER_SOCKET-<unset>}" "${ADB_USB-<unset>}" \
    "${ADB_MDNS_AUTO_CONNECT-<unset>}" >"${FUNK_TEST_SCRCPY_ENV_LOG:?}"
