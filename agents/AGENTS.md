# Repository guidance files

When persistent repository guidance is needed in a project, create or
update `AGENTS.md` as the canonical source, then create `CLAUDE.md` as a
symlink to it (`ln -s AGENTS.md CLAUDE.md`).
