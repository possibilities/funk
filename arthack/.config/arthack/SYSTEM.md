## System

- `~/src` — clones of other people's projects: upstream sources, forks, and
  checkouts kept for research. New clones belong here too.
- `~/code` — Mike's own projects, source and otherwise. A bare project name in
  a request usually means a directory here.
